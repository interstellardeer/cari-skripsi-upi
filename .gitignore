node_modules/

# Environment variables — never commit real values
.env*
# ...but always allow the example templates
!.env.example

.next/
out/

# Dataset files — download separately, never commit
data/
*.parquet
*.zip

# TypeScript build artifacts
*.tsbuildinfo

# Tooling
.turbo
.vercel
